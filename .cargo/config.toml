# Local CI is primary. Remote GitHub Actions are disabled (manual dispatch only).
[alias]
f = ["fmt", "--all"]
t = "test"
ci = "test"
# Embedded checks (run separately; cargo aliases cannot chain targets easily):
#   cargo check --target thumbv7em-none-eabi
#   cargo check --target thumbv6m-none-eabi --features portable-atomic-unsafe-assume-single-core
